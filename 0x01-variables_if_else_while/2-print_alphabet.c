#include <stdio.h>

/**
 *main - print the alphabet
 *
 *return: Always 0 (success)
 */

int main(void)
{
		char abc;
		for (abc = 'a'; abc <= 'z'; abc++)
		putchar (abc);
		putchar ('\n');
		
		return (0);

}
